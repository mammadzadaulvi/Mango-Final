@using Mango.Areas.Admin.ViewModels.WomenOtherLine
@model WomenOtherLineIndexVM


<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Women Other Line</h4>
                        <a class="btn btn-primary" asp-action="create" asp-controller="womenotherline" asp-area="admin">CREATE Women Other Line</a>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Title</th>
                                        <th>Url</th>
                                        <th>Photo</th>
                                        <th>Actions</th>

                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var otherLine in Model.WomenOtherLines)
                                    {
                                        <tr>
                                            <td>@otherLine.Id</td>
                                            <td>@otherLine.Title</td>
                                            <td>@otherLine.Url</td>
                                            <td>
                                                <div style="width:250px">
                                                    <img style="max-width:100%" src="~/assets/img/@otherLine.PhotoPath" />
                                                </div>
                                            </td>
                                            <td>
                                                <a class="btn btn-primary" asp-action="update" asp-controller="womenotherline" asp-area="admin" asp-route-id="@otherLine.Id">Update</a>
                                                <a class="btn btn-danger" onclick="confirm('Eminsinizmi?')" asp-action="delete" asp-controller="womenotherline" asp-area="admin" asp-route-id="@otherLine.Id">Delete</a>
                                                <a class="btn btn-info" asp-action="details" asp-controller="womenotherline" asp-area="admin" asp-route-id="@otherLine.Id">Details</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>