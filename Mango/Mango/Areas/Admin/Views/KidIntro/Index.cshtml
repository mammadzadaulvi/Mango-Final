@using Mango.Areas.Admin.ViewModels.KidIntro
@model KidIntroIndexVm


<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Kid Intro</h4>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Title</th>
                                        <th>Description</th>
                                        <th>Url</th>
                                        <th>Photo</th>
                                        <th>Actions</th>

                                    </tr>
                                </thead>
                                <tbody>

                                    @{
                                        @if (Model.KidIntro == null)
                                        {

                                            <a class="btn btn-primary" asp-action="create" asp-controller="kidintro" asp-area="admin">CREATE Kid Intro</a>

                                        }
                                        else
                                        {
                                            <tr>
                                                <td>@Model.KidIntro.Id</td>
                                                <td>@Model.KidIntro.Title</td>
                                                <td>@Model.KidIntro.Description</td>
                                                <td>@Model.KidIntro.Url</td>
                                                <td><img src="~/assets/img/@Model.KidIntro.PhotoPath" /></td>
                                                <td>
                                                    <a class="btn btn-primary" asp-action="update" asp-controller="kidintro" asp-area="admin" asp-route-id="@Model.KidIntro.Id">Update</a>
                                                    <a class="btn btn-danger" onclick="confirm('Eminsinizmi?')" asp-action="delete" asp-controller="kidintro" asp-area="admin" asp-route-id="@Model.KidIntro.Id">Delete</a>
                                                    <a class="btn btn-info" asp-action="details" asp-controller="kidintro" asp-area="admin" asp-route-id="@Model.KidIntro.Id">Details</a>
                                                </td>
                                            </tr>
                                        }
                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

