@using Mango.Areas.Admin.ViewModels.ProductSubCategory
@model ProductSubCategoryIndexVM


<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Product SubCategory</h4>
                        <a class="btn btn-primary" asp-action="create" asp-controller="productsubcategory" asp-area="admin">CREATE Product Category</a>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Title</th>
                                        <th>ProductCategoryId</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var otherLine in Model.ProductSubCategories)
                                    {
                                        <tr>
                                            <td>@otherLine.Id</td>
                                            <td>@otherLine.Title</td>
                                            <td>@otherLine.ProductCategoryId</td>
                                            <td>
                                                <a class="btn btn-primary" asp-action="update" asp-controller="productsubcategory" asp-area="admin" asp-route-id="@otherLine.Id">Update</a>
                                                <a class="btn btn-danger" onclick="confirm('Eminsinizmi?')" asp-action="delete" asp-controller="productsubcategory" asp-area="admin" asp-route-id="@otherLine.Id">Delete</a>
                                                <a class="btn btn-info" asp-action="details" asp-controller="productsubcategory" asp-area="admin" asp-route-id="@otherLine.Id">Details</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>