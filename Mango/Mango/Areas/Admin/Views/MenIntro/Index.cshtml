@using Mango.Areas.Admin.ViewModels.MenIntro
@model MenIntroIndexVM


<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Men Intro</h4>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Title</th>
                                        <th>Description</th>
                                        <th>Url</th>
                                        <th>Photo</th>
                                        <th>Actions</th>

                                    </tr>
                                </thead>
                                <tbody>

                                    @{
                                        @if (Model.MenIntro == null)
                                        {

                                            <a class="btn btn-primary" asp-action="create" asp-controller="menintro" asp-area="admin">CREATE Men Intro</a>

                                        }
                                        else
                                        {
                                            <tr>
                                                <td>@Model.MenIntro.Id</td>
                                                <td>@Model.MenIntro.Title</td>
                                                <td>@Model.MenIntro.Description</td>
                                                <td>@Model.MenIntro.Url</td>
                                                <td>
                                                    <div style="width:300px">
                                                        <img style="max-width:100%" src="~/assets/img/@Model.MenIntro.PhotoPath" />
                                                    </div>
                                                
                                                </td>
                                                <td>
                                                    <a class="btn btn-primary" asp-action="update" asp-controller="menintro" asp-area="admin" asp-route-id="@Model.MenIntro.Id">Update</a>
                                                    <a class="btn btn-danger" onclick="confirm('Eminsinizmi?')" asp-action="delete" asp-controller="menintro" asp-area="admin" asp-route-id="@Model.MenIntro.Id">Delete</a>
                                                    <a class="btn btn-info" asp-action="details" asp-controller="menintro" asp-area="admin" asp-route-id="@Model.MenIntro.Id">Details</a>
                                                </td>
                                            </tr>
                                        }
                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

